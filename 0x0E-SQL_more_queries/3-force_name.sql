-- This command create table 'force_name' if it doesn't exist
-- with id INT and name VARCHAR NOT NULL attributes 
CREATE TABLE IF NOT EXISTS `force_name` (
    `id` INT,
    `name` VARCHAR(256) NOT NULL
);

