-- SQL query updates the score column in the second_table table,
-- setting the value to 10 for the row where the name column
-- is equal to 'Bob'.
UPDATE `second_table` SET `score` = 10 WHERE `name` = 'Bob';
